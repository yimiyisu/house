[Unit]
Description = house server
After = network.target syslog.target
Wants = network.target

[Service]
Type = simple
Restart=always
WorkingDirectory = /data/apps/house
ExecStart = java  -Dfile.encoding=UTF-8 -server -XX:+UseG1GC -XX:+TieredCompilation  -Xms512m -Xmx512m -jar /data/apps/house/house.jar
[Install]
WantedBy = multi-user.target